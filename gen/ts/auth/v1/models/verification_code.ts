// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.1
//   protoc               unknown
// source: auth/v1/models/verification_code.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export interface VerificationCode {
  $type: "auth.v1.models.VerificationCode";
  id: string;
  employeeId: string;
  contactType: string;
  contact: string;
  expiresAt: number;
  verifiedAt: number;
  attempts: number;
}

function createBaseVerificationCode(): VerificationCode {
  return {
    $type: "auth.v1.models.VerificationCode",
    id: "",
    employeeId: "",
    contactType: "",
    contact: "",
    expiresAt: 0,
    verifiedAt: 0,
    attempts: 0,
  };
}

export const VerificationCode: MessageFns<VerificationCode, "auth.v1.models.VerificationCode"> = {
  $type: "auth.v1.models.VerificationCode" as const,

  encode(message: VerificationCode, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.employeeId !== "") {
      writer.uint32(18).string(message.employeeId);
    }
    if (message.contactType !== "") {
      writer.uint32(26).string(message.contactType);
    }
    if (message.contact !== "") {
      writer.uint32(34).string(message.contact);
    }
    if (message.expiresAt !== 0) {
      writer.uint32(40).int64(message.expiresAt);
    }
    if (message.verifiedAt !== 0) {
      writer.uint32(48).int64(message.verifiedAt);
    }
    if (message.attempts !== 0) {
      writer.uint32(56).int32(message.attempts);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): VerificationCode {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseVerificationCode();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.employeeId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.contactType = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.contact = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.expiresAt = longToNumber(reader.int64());
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.verifiedAt = longToNumber(reader.int64());
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.attempts = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): VerificationCode {
    return {
      $type: VerificationCode.$type,
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      employeeId: isSet(object.employeeId)
        ? globalThis.String(object.employeeId)
        : isSet(object.employee_id)
        ? globalThis.String(object.employee_id)
        : "",
      contactType: isSet(object.contactType)
        ? globalThis.String(object.contactType)
        : isSet(object.contact_type)
        ? globalThis.String(object.contact_type)
        : "",
      contact: isSet(object.contact) ? globalThis.String(object.contact) : "",
      expiresAt: isSet(object.expiresAt)
        ? globalThis.Number(object.expiresAt)
        : isSet(object.expires_at)
        ? globalThis.Number(object.expires_at)
        : 0,
      verifiedAt: isSet(object.verifiedAt)
        ? globalThis.Number(object.verifiedAt)
        : isSet(object.verified_at)
        ? globalThis.Number(object.verified_at)
        : 0,
      attempts: isSet(object.attempts) ? globalThis.Number(object.attempts) : 0,
    };
  },

  toJSON(message: VerificationCode): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.employeeId !== "") {
      obj.employeeId = message.employeeId;
    }
    if (message.contactType !== "") {
      obj.contactType = message.contactType;
    }
    if (message.contact !== "") {
      obj.contact = message.contact;
    }
    if (message.expiresAt !== 0) {
      obj.expiresAt = Math.round(message.expiresAt);
    }
    if (message.verifiedAt !== 0) {
      obj.verifiedAt = Math.round(message.verifiedAt);
    }
    if (message.attempts !== 0) {
      obj.attempts = Math.round(message.attempts);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<VerificationCode>, I>>(base?: I): VerificationCode {
    return VerificationCode.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<VerificationCode>, I>>(object: I): VerificationCode {
    const message = createBaseVerificationCode();
    message.id = object.id ?? "";
    message.employeeId = object.employeeId ?? "";
    message.contactType = object.contactType ?? "";
    message.contact = object.contact ?? "";
    message.expiresAt = object.expiresAt ?? 0;
    message.verifiedAt = object.verifiedAt ?? 0;
    message.attempts = object.attempts ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string } ? { [K in keyof Omit<T, "$case">]?: DeepPartial<T[K]> } & { $case: T["$case"] }
  : T extends {} ? { [K in Exclude<keyof T, "$type">]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P> | "$type">]: never };

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

interface MessageFns<T, V extends string> {
  readonly $type: V;
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
