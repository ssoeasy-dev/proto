// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.6.1
//   protoc               unknown
// source: auth/v1/verification.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { type CallContext, type CallOptions } from "nice-grpc-common";

export interface GenerateVerificationCodeRequest {
  $type: "auth.v1.GenerateVerificationCodeRequest";
  employeeId: string;
  email: string;
}

export interface GenerateVerificationCodeResponse {
  $type: "auth.v1.GenerateVerificationCodeResponse";
  code: string;
  employeeId: string;
  email: string;
  expiresAt: number;
}

function createBaseGenerateVerificationCodeRequest(): GenerateVerificationCodeRequest {
  return { $type: "auth.v1.GenerateVerificationCodeRequest", employeeId: "", email: "" };
}

export const GenerateVerificationCodeRequest: MessageFns<
  GenerateVerificationCodeRequest,
  "auth.v1.GenerateVerificationCodeRequest"
> = {
  $type: "auth.v1.GenerateVerificationCodeRequest" as const,

  encode(message: GenerateVerificationCodeRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.employeeId !== "") {
      writer.uint32(10).string(message.employeeId);
    }
    if (message.email !== "") {
      writer.uint32(18).string(message.email);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GenerateVerificationCodeRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGenerateVerificationCodeRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.employeeId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.email = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GenerateVerificationCodeRequest {
    return {
      $type: GenerateVerificationCodeRequest.$type,
      employeeId: isSet(object.employeeId) ? globalThis.String(object.employeeId) : "",
      email: isSet(object.email) ? globalThis.String(object.email) : "",
    };
  },

  toJSON(message: GenerateVerificationCodeRequest): unknown {
    const obj: any = {};
    if (message.employeeId !== "") {
      obj.employeeId = message.employeeId;
    }
    if (message.email !== "") {
      obj.email = message.email;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GenerateVerificationCodeRequest>, I>>(base?: I): GenerateVerificationCodeRequest {
    return GenerateVerificationCodeRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GenerateVerificationCodeRequest>, I>>(
    object: I,
  ): GenerateVerificationCodeRequest {
    const message = createBaseGenerateVerificationCodeRequest();
    message.employeeId = object.employeeId ?? "";
    message.email = object.email ?? "";
    return message;
  },
};

function createBaseGenerateVerificationCodeResponse(): GenerateVerificationCodeResponse {
  return { $type: "auth.v1.GenerateVerificationCodeResponse", code: "", employeeId: "", email: "", expiresAt: 0 };
}

export const GenerateVerificationCodeResponse: MessageFns<
  GenerateVerificationCodeResponse,
  "auth.v1.GenerateVerificationCodeResponse"
> = {
  $type: "auth.v1.GenerateVerificationCodeResponse" as const,

  encode(message: GenerateVerificationCodeResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.code !== "") {
      writer.uint32(10).string(message.code);
    }
    if (message.employeeId !== "") {
      writer.uint32(18).string(message.employeeId);
    }
    if (message.email !== "") {
      writer.uint32(26).string(message.email);
    }
    if (message.expiresAt !== 0) {
      writer.uint32(32).int64(message.expiresAt);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GenerateVerificationCodeResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGenerateVerificationCodeResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.code = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.employeeId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.email = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.expiresAt = longToNumber(reader.int64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GenerateVerificationCodeResponse {
    return {
      $type: GenerateVerificationCodeResponse.$type,
      code: isSet(object.code) ? globalThis.String(object.code) : "",
      employeeId: isSet(object.employeeId) ? globalThis.String(object.employeeId) : "",
      email: isSet(object.email) ? globalThis.String(object.email) : "",
      expiresAt: isSet(object.expiresAt) ? globalThis.Number(object.expiresAt) : 0,
    };
  },

  toJSON(message: GenerateVerificationCodeResponse): unknown {
    const obj: any = {};
    if (message.code !== "") {
      obj.code = message.code;
    }
    if (message.employeeId !== "") {
      obj.employeeId = message.employeeId;
    }
    if (message.email !== "") {
      obj.email = message.email;
    }
    if (message.expiresAt !== 0) {
      obj.expiresAt = Math.round(message.expiresAt);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GenerateVerificationCodeResponse>, I>>(
    base?: I,
  ): GenerateVerificationCodeResponse {
    return GenerateVerificationCodeResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GenerateVerificationCodeResponse>, I>>(
    object: I,
  ): GenerateVerificationCodeResponse {
    const message = createBaseGenerateVerificationCodeResponse();
    message.code = object.code ?? "";
    message.employeeId = object.employeeId ?? "";
    message.email = object.email ?? "";
    message.expiresAt = object.expiresAt ?? 0;
    return message;
  },
};

export type VerificationServiceDefinition = typeof VerificationServiceDefinition;
export const VerificationServiceDefinition = {
  name: "VerificationService",
  fullName: "auth.v1.VerificationService",
  methods: {
    generateVerificationCode: {
      name: "GenerateVerificationCode",
      requestType: GenerateVerificationCodeRequest,
      requestStream: false,
      responseType: GenerateVerificationCodeResponse,
      responseStream: false,
      options: {},
    },
  },
} as const;

export interface VerificationServiceImplementation<CallContextExt = {}> {
  generateVerificationCode(
    request: GenerateVerificationCodeRequest,
    context: CallContext & CallContextExt,
  ): Promise<DeepPartial<GenerateVerificationCodeResponse>>;
}

export interface VerificationServiceClient<CallOptionsExt = {}> {
  generateVerificationCode(
    request: DeepPartial<GenerateVerificationCodeRequest>,
    options?: CallOptions & CallOptionsExt,
  ): Promise<GenerateVerificationCodeResponse>;
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string } ? { [K in keyof Omit<T, "$case">]?: DeepPartial<T[K]> } & { $case: T["$case"] }
  : T extends {} ? { [K in Exclude<keyof T, "$type">]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P> | "$type">]: never };

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

interface MessageFns<T, V extends string> {
  readonly $type: V;
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
