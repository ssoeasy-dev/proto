// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.6.1
//   protoc               unknown
// source: auth/v1/create_credential.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Credential } from "./models/credential";

export interface CreateCredentialsRequest {
  $type: "auth.v1.CreateCredentialsRequest";
  employeeId: string;
  password: string;
}

export interface CreateCredentialsResponse {
  $type: "auth.v1.CreateCredentialsResponse";
  credential?: Credential | undefined;
}

export interface CreateCredentialsCompinsateRequest {
  $type: "auth.v1.CreateCredentialsCompinsateRequest";
  credentialId: string;
}

export interface CreateCredentialsCompinsateResponse {
  $type: "auth.v1.CreateCredentialsCompinsateResponse";
  success: boolean;
  message: string;
}

function createBaseCreateCredentialsRequest(): CreateCredentialsRequest {
  return { $type: "auth.v1.CreateCredentialsRequest", employeeId: "", password: "" };
}

export const CreateCredentialsRequest: MessageFns<CreateCredentialsRequest, "auth.v1.CreateCredentialsRequest"> = {
  $type: "auth.v1.CreateCredentialsRequest" as const,

  encode(message: CreateCredentialsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.employeeId !== "") {
      writer.uint32(10).string(message.employeeId);
    }
    if (message.password !== "") {
      writer.uint32(18).string(message.password);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CreateCredentialsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCreateCredentialsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.employeeId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.password = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CreateCredentialsRequest {
    return {
      $type: CreateCredentialsRequest.$type,
      employeeId: isSet(object.employeeId) ? globalThis.String(object.employeeId) : "",
      password: isSet(object.password) ? globalThis.String(object.password) : "",
    };
  },

  toJSON(message: CreateCredentialsRequest): unknown {
    const obj: any = {};
    if (message.employeeId !== "") {
      obj.employeeId = message.employeeId;
    }
    if (message.password !== "") {
      obj.password = message.password;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CreateCredentialsRequest>, I>>(base?: I): CreateCredentialsRequest {
    return CreateCredentialsRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CreateCredentialsRequest>, I>>(object: I): CreateCredentialsRequest {
    const message = createBaseCreateCredentialsRequest();
    message.employeeId = object.employeeId ?? "";
    message.password = object.password ?? "";
    return message;
  },
};

function createBaseCreateCredentialsResponse(): CreateCredentialsResponse {
  return { $type: "auth.v1.CreateCredentialsResponse", credential: undefined };
}

export const CreateCredentialsResponse: MessageFns<CreateCredentialsResponse, "auth.v1.CreateCredentialsResponse"> = {
  $type: "auth.v1.CreateCredentialsResponse" as const,

  encode(message: CreateCredentialsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.credential !== undefined) {
      Credential.encode(message.credential, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CreateCredentialsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCreateCredentialsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.credential = Credential.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CreateCredentialsResponse {
    return {
      $type: CreateCredentialsResponse.$type,
      credential: isSet(object.credential) ? Credential.fromJSON(object.credential) : undefined,
    };
  },

  toJSON(message: CreateCredentialsResponse): unknown {
    const obj: any = {};
    if (message.credential !== undefined) {
      obj.credential = Credential.toJSON(message.credential);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CreateCredentialsResponse>, I>>(base?: I): CreateCredentialsResponse {
    return CreateCredentialsResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CreateCredentialsResponse>, I>>(object: I): CreateCredentialsResponse {
    const message = createBaseCreateCredentialsResponse();
    message.credential = (object.credential !== undefined && object.credential !== null)
      ? Credential.fromPartial(object.credential)
      : undefined;
    return message;
  },
};

function createBaseCreateCredentialsCompinsateRequest(): CreateCredentialsCompinsateRequest {
  return { $type: "auth.v1.CreateCredentialsCompinsateRequest", credentialId: "" };
}

export const CreateCredentialsCompinsateRequest: MessageFns<
  CreateCredentialsCompinsateRequest,
  "auth.v1.CreateCredentialsCompinsateRequest"
> = {
  $type: "auth.v1.CreateCredentialsCompinsateRequest" as const,

  encode(message: CreateCredentialsCompinsateRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.credentialId !== "") {
      writer.uint32(10).string(message.credentialId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CreateCredentialsCompinsateRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCreateCredentialsCompinsateRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.credentialId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CreateCredentialsCompinsateRequest {
    return {
      $type: CreateCredentialsCompinsateRequest.$type,
      credentialId: isSet(object.credentialId) ? globalThis.String(object.credentialId) : "",
    };
  },

  toJSON(message: CreateCredentialsCompinsateRequest): unknown {
    const obj: any = {};
    if (message.credentialId !== "") {
      obj.credentialId = message.credentialId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CreateCredentialsCompinsateRequest>, I>>(
    base?: I,
  ): CreateCredentialsCompinsateRequest {
    return CreateCredentialsCompinsateRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CreateCredentialsCompinsateRequest>, I>>(
    object: I,
  ): CreateCredentialsCompinsateRequest {
    const message = createBaseCreateCredentialsCompinsateRequest();
    message.credentialId = object.credentialId ?? "";
    return message;
  },
};

function createBaseCreateCredentialsCompinsateResponse(): CreateCredentialsCompinsateResponse {
  return { $type: "auth.v1.CreateCredentialsCompinsateResponse", success: false, message: "" };
}

export const CreateCredentialsCompinsateResponse: MessageFns<
  CreateCredentialsCompinsateResponse,
  "auth.v1.CreateCredentialsCompinsateResponse"
> = {
  $type: "auth.v1.CreateCredentialsCompinsateResponse" as const,

  encode(message: CreateCredentialsCompinsateResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.success !== false) {
      writer.uint32(8).bool(message.success);
    }
    if (message.message !== "") {
      writer.uint32(18).string(message.message);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CreateCredentialsCompinsateResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCreateCredentialsCompinsateResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.success = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.message = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CreateCredentialsCompinsateResponse {
    return {
      $type: CreateCredentialsCompinsateResponse.$type,
      success: isSet(object.success) ? globalThis.Boolean(object.success) : false,
      message: isSet(object.message) ? globalThis.String(object.message) : "",
    };
  },

  toJSON(message: CreateCredentialsCompinsateResponse): unknown {
    const obj: any = {};
    if (message.success !== false) {
      obj.success = message.success;
    }
    if (message.message !== "") {
      obj.message = message.message;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CreateCredentialsCompinsateResponse>, I>>(
    base?: I,
  ): CreateCredentialsCompinsateResponse {
    return CreateCredentialsCompinsateResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CreateCredentialsCompinsateResponse>, I>>(
    object: I,
  ): CreateCredentialsCompinsateResponse {
    const message = createBaseCreateCredentialsCompinsateResponse();
    message.success = object.success ?? false;
    message.message = object.message ?? "";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string } ? { [K in keyof Omit<T, "$case">]?: DeepPartial<T[K]> } & { $case: T["$case"] }
  : T extends {} ? { [K in Exclude<keyof T, "$type">]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P> | "$type">]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

interface MessageFns<T, V extends string> {
  readonly $type: V;
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
