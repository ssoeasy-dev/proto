syntax = "proto3";

package companies.v1;

import "companies/v1/registration.proto";

service CompanyService {
  rpc RegisterUser(RegisterUserRequest) returns (RegisterUserResponse);
  rpc RegisterUserCompinsate(RegisterUserCompinsateRequest) returns (RegisterUserCompinsateResponse);
}
